# Extras

A drop-in folder for any extra content worth sharing with the cohort — setup guides,
reference sheets, recordings, datasets, useful links, etc.

## How to add something
1. Place the file in this folder (or add a link below).
2. Add a matching row to the table in `extras.html` so it shows up on the website
   (a commented-out template row is provided there).
